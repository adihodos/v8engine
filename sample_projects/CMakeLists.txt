if (MSVC)
	#add_subdirectory(lighting)
	#add_subdirectory(simple_animation)
	add_subdirectory(basic_drawing)
	add_subdirectory(julia_fractal)
    #add_subdirectory(test)
endif(MSVC)

if (MINGW)
    add_subdirectory(basic_opengl)
endif(MINGW)
